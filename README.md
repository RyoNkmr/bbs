# oldskool BBS

![screenshot](./screenshot.png)

## Run

```bash
$ cargo run
```

## Setup for Development

- install diesel cli: `cargo install diesel_cli`
  - use diesel cli to migrate database: `diesel migration ...`
